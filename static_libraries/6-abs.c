#include "main.h"
/**
 * _abs - shows absolute value of integer
 * @n: computed integer
 * Return: absolute value of integer success style
 */
int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
